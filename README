How to run:

Start MongoDB locally (e.g., mongod or use Atlas)

cd backend → npm install → create .env → npx nodemon server.js

Open frontend HTML files in browser (or serve frontend folder with a simple server)

Endpoints available today:

POST /api/register – create user (returns token)

POST /api/login –