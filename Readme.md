How to run:

Start MongoDB locally , use your own mongo db link and keep it in CONNECTION_STRING inside .env file, Set ACCESS_TOKEN_SECRET = 'Suniley19' or YOUR CHOICE , set MAX_SIZE

cd backend → npm install → npx nodemon server.js

